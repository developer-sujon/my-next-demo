import Posts from "@/components/posts/Post";

const Parallel = () => {
  return (
    <div>
      Parallel Posts
      <Posts linkToRedirect="/parallel" />
    </div>
  );
};

export default Parallel;
